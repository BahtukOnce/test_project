# Ядро симуляции и обучения
mujoco>=3.1,<4
gymnasium>=1.0,<2
stable-baselines3>=2.3,<3
numpy>=1.26

# torch ставится отдельно (см. setup.sh): CPU-сборка вместо CUDA,
# иначе pip тянет ~2.5 ГБ драйверов, которые на сервере без видеокарты не нужны.
torch>=2.2

# Запись видео и HUD
imageio>=2.34
imageio-ffmpeg>=0.4.9
pillow>=10.0

# Графики обучения
matplotlib>=3.8
